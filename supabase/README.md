# Supabase

Projeto remoto:

- URL: `https://tylzdvyaraowcbptrctr.supabase.co`
- Project ref: `tylzdvyaraowcbptrctr`

O Supabase CLI global nao estava instalado no ambiente. Use `npx supabase` ou instale o CLI globalmente.

```bash
npx supabase login
npx supabase link --project-ref tylzdvyaraowcbptrctr
```

O backend usa o Postgres remoto via `DATABASE_URL` no arquivo `.env`.
